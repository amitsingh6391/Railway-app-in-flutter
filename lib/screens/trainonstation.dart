import "package:flutter/material.dart";

class Trainonstation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      
      child:Text("train on station")
    );
  }
}